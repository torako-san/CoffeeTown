<html>
  <body>
    <main>
    <div class="top-img mb-2">
      <section class="text-center container">
          <div class="row py-lg-5">
            <div class="col-lg-6 col-md-8 mx-auto">
              <h2 class="fw-light">最高を一杯をシェアしよう</h2>
                <p class="sub-text lead">あなたの秘密のお店や美味しい一杯を教えてください.</p>
                <p>
                  <%= link_to 'Main call to action', root_path, class:"btn btn-primary my-2" %>
                  <%= link_to 'Secondary action', root_path, class:"btn btn-secondary my-2" %>
                </p>
            </div>
          </div>
        </div>
      </section>
      </div>

      <%# 店舗一覧 %>
      <% if @shop.present? %>
          <div class="shop-list py-5 bg-light">
            <div class="container">
            <div class="row">
              <% @shop.each do |shop| %>
                <div class="col-lg-3 col-md-6 mb-4">
                  <div class="col">
                    <div class="card shadow-sm">
                        <%= image_tag shop.production.image, class:"production-img bd-placeholder-img card-img-top", width:"100%", height:"225" if shop.production.image.attached? %>
                  
                        <div class="card-body">
                          <p class="card-text">■投稿日<br><%= shop.created_at.strftime('%Y/%m/%d') %></p>
                          <p class="card-text">■店名<br><%= link_to shop.shop_name, "#", class:"shop-text" %></p>
                          <p class="card-text">■商品名<br><%= shop.production.production_name %></p>
                          <p class="card-text">■商品カテゴリー<br><%= shop.production.product_category.name %></p>
                        <div class="d-flex justify-content-between align-items-center">
                          <div class="btn-group">
                            <%= link_to 'View', "#", class:"btn btn-sm btn-outline-secondary" %>
                            <%= link_to 'Edit', "#", class:"btn btn-sm btn-outline-secondary" %>
                          </div>
                          <%= link_to shop.user.nickname, root_path, class:"text-muted" %>
                        </div>
                      </div>
                    </div>
                  </div>
                </div>
              <% end %>
            </div>
          </div>
        </div>  
      <% else %>
        <%= render partial: 'index' %>
      <% end %>
    </main> 
    <%# /店舗一覧 %>
  </body>
</html>
